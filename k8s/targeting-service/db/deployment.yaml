apiVersion: apps/v1
kind: Deployment
metadata:
  name: targeting-db
  namespace: togglemaster
spec:
  selector:
    matchLabels:
      app: targeting-db
  template:
    metadata:
      labels:
        app: targeting-db
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
          - name: POSTGRES_USER
            valueFrom:
              secretKeyRef:
                name: targeting-db-secret
                key: POSTGRES_USER
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: targeting-db-secret
                key: POSTGRES_PASSWORD
          - name: POSTGRES_DB
            valueFrom:
              secretKeyRef:
                name: targeting-db-secret
                key: POSTGRES_DB
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: init-sql
              mountPath: /docker-entrypoint-initdb.d/init.sql
              subPath: init.sql
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: targeting-db-pvc
        - name: init-sql
          configMap:
            name: targeting-db-init
